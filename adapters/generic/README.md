# generic adapter

The generic MCP server is implemented in `src/epistemos/mcp/`. Spec: `docs/integration/GENERIC_MCP_SPEC.md`.
Dependency direction: adapter depends on EPISTEMOS; the core never imports it.
